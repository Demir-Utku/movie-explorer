{
  "extends": "./tsconfig.app.json",
  // TODO: Add `"./__tests__/*"` to the `include` array when you have tests
  "include": ["env.d.ts"],
  "exclude": [],
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.vitest.tsbuildinfo",

    "lib": [],
    "types": ["node", "jsdom"]
  }
}
